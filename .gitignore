.venv/
__pycache__/
.pytest_cache/
*.egg-info/
.DS_Store
.claude/

# Licensed vocabulary data and any real/de-identified patient-level fixtures --
# must never be committed (see README's "Data governance" section).
athena/
scratchpad_ukb/
*deid*.csv

# Local source downloads, generated releases, reports, and cohort runs. The
# shareable distribution archive under distributions/ is tracked separately.
hierarchy_sources/
releases/
runs/
reports/
phecodex_results_*/
phecodeX_*.csv
phecodeX_*_manifest.json
