.venv/
__pycache__/
.pytest_cache/
*.egg-info/
build/
dist/
.DS_Store
.claude/

# Licensed vocabulary data and any real/de-identified patient-level fixtures --
# must never be committed (see README's "Data governance" section).
athena/
scratchpad_ukb/
*deid*.csv

# Local source downloads, generated releases, reports, and cohort runs.
hierarchy_sources/
releases/
runs/
reports/
phecodex_results_*/
phecodeX_*.csv
phecodeX_*_manifest.json

# Analyst bundles. Derived output, regenerable from this repo plus the PhecodeX
# source maps, and large enough that committing one would sit in git history
# permanently. Publish them as GitHub Releases instead, with the .sha256 sidecar
# attached alongside the archive.
distributions/
